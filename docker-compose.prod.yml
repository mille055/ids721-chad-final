version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: patient-converter
    ports:
      - "8000:8000"
    environment:
      - LLAMA_SERVER_URL=http://<model-server-ip>:8080
